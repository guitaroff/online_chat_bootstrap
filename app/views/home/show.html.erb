<% if user_signed_in? %>
  <%= turbo_stream_from :rooms %>

  <%= render @rooms %>

  <%= form_with url: rooms_path, scope: :room, method: :post do |f| %>
    <%= f.text_field :title %>
    <%= f.submit 'Создать комнату' %>
  <% end %>
<% end %>
